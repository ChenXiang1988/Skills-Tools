interface:
  display_name: "PM Flow"
  short_description: "Orchestrate product workflow stages, workspace and knowledge-base gates, artifacts, and decisions."
  default_prompt: "Use $pm-flow to start or resume a gated product requirement workflow with workspace and knowledge-base confirmation."
