# 2026-07-17

## 融合 pm-flow-kit-codex 优点到 pm-flow-kit-workbuddy（忠实搬回 / 方案 A）

用户选择"忠实搬回"（绑定治理用专用子命令落盘，非 agent 直写）。已落地：

### 改动清单
1. **3 个专项 skill 移植**：从 codex 复制 `skills/pm-research`、`pm-boundary`、`pm-review`（各含 SKILL.md + references/*-method.md + assets/templates/*.md + agents/openai.yaml）到 workbuddy。去 Codex 化：仅改 SKILL.md `description` 的 `Use when Codex needs to` → `Use when you need to`。stages 模板（pm-flow/stages/{research,boundary,review}/template.md）保留不删（与 codex 一致，属叠加而非重复）。
2. **路由更新**：`pm-flow/SKILL.md` Skill Routing + `pm-flow/USAGE.md` Stage Guidance 增加 3 个 skill 指向。
3. **AGENT.md 新建**：取 codex 版，仅改写 Local Update Boundary 段（去 Codex 语义，改 WorkBuddy 本地改动 vs 发布语境）。
4. **绑定治理（核心，方案 A）**：
   - `pm-flow/controller.py` 插入干净绑定块（helper + cmd_workspace_*/cmd_knowledge_*），含常量 `WORKSPACE_DEFAULT_WORKDIR`、`KNOWLEDGE_BASE_DEFAULT_MODE`、`canonical_root`。行为：有有效绑定且路径匹配静默跳过；无绑定问一次；变更停下澄清/rebind。
   - `main()` 对非 init/attach/workspace/knowledge 命令强制 `assert_workspace_current`/`assert_knowledge_base_current`。
   - `shared/project_config_schema.json` 补 `workspace`/`knowledge_base` 字段。
   - `contract_config.json` 补 2 条全局 semantic_checks（工作区/知识库已确认）。
5. **KIT.md 新建**：codex 版去 Codex 化 + 补充 workbuddy 独有 `design-system/` 说明。
6. **manifest.json**：版本 bump 0.1.0→0.2.0；`skills` 数组补 3 项；`constraints`/`constraints_zh` 补 research/boundary/review 路由；加 `upstream`+`upstream_synced_at`；保留 `build: workbuddy` 与 `bundled_assets`（design-system）。
7. **Stage 0 同步**：codex 给 requirement_analysis 加了 "## 9. Deferred Or Non-Applicable Decisions"（常量 + 模板均同步，Source Error 顺延为第 10 节）。

### 关键决策/踩坑
- codex controller.py 部分非绑定字符串被污染（夹带 "text" 乱码 + 中文标点，如 `die("text state.json（%s）。text init。")`），workbuddy 这些段是干净英文。因此**不能整文件覆盖 codex**，只插入干净的绑定块，保留了 workbuddy 的干净字符串。
- 未移植 `.codex-plugin/plugin.json`（Codex 平台清单）。未删 workbuddy 独有 `design-system/`。
- 全包残留 "Codex" 仅 `manifest.json` 的 `upstream` 字段与 `KIT.md` 来源说明，均为有意的来源记录。

### 验证
- `controller.py` 编译通过；`workspace bind`/`knowledge bind --none`/`status`/`validate` 全链路 exit=0；工作区变更 mismatch 正确拦截；project_config.json 正确写入 workspace + knowledge_base 双绑定。

## 17:20 补回 2 块真实落后项（重新评估发现融合时遗漏）

用户给出"workbuddy 落后 codex"评估，重新核查后确认融合时漏了 2 块，已补齐：

1. **contract stages 门禁未引用新搬回的专项 skill**：research/boundary 缺 "pm-research/pm-boundary was used" 检查，review 还指向旧的 `subagent-review.md`。已在 `contract_config.json` 三段各补 1 条专项 skill 使用门禁。
2. **validate.py 缺 PRD/review 最低内容完整性检查**：workbuddy 原 528 行，codex 771 行。从 codex 干净移植整条链（避开 text 乱码）：`effective_stage_artifacts`/`matched_artifact_paths`/`_read_first_stage_artifact`/`_line_value`/`_has_file_or_non_applicability`/`_validate_prd_content`/`_validate_review_content`/`validate_stage_content`，加 `import re`，并在 `run_validation` 4b 块后挂载调用。

### 当前对齐结论
- **已对齐 codex**：绑定治理、3 专项 skill、AGENT/KIT/manifest、validate 命令、PRD/review 内容校验、contract stages 专项 skill 门禁。
- **workbuddy 领先 codex（非落后）**：`design-system/` 资产、`pm-html-prototype` 的 design-system 适配（prototype-chrome.css 等 workbuddy 独有）、`bundled_assets` 元数据、KIT.md 产品化描述。
- **workbuddy 自身问题（非落后 codex）**：`smoke_test.py` 跑不通（未确认 workspace 就 scaffold）；分发包需清理 `.DS_Store`/`__pycache__`（`.workbuddy/` 不删）。

### 验证（本轮）
- `validate.py` 编译通过；`contract_config.json` JSON 合法。
- 函数级：合法 PRD/review → 0 错误；残缺 → 5~6 错误。
- 端到端：prd done + 占位 prd.md → 命中 6 错误，validate failed。代码内 `validate_stage_content` 仅调用 1 次（579 行），无重复。

## 17:33 修复 smoke_test + 清理分发目录（用户确认"需要"）

### 根因（smoke_test 之前跑不通）
1. `cmd_init`/`cmd_attach` 未自动绑定 workspace/knowledge → scaffold 等命令触发门禁 die。
2. `cmd_mark`/`cmd_close` 未做 PRD/review 内容门禁（`ensure_stage_content_ready` 缺失）→ `mark prd` 在内容不足时意外通过。
3. `validate.run_validation` 只在 `done/closed` 做 PRD/review 内容校验，漏了 `confirmed` 状态（codex 703 行有第二处调用）。
4. `contract_config.json` 落后于 codex：缺 fixed-full-workflow、duplicate-action、subjective-only 等 gate，且 workspace/knowledge 段措辞未更新到 codex 新版（4 条）。
5. `smoke_test.py` 是 codex 旧版，缺 `bind_no_knowledge_base`/`test_knowledge_rebind` 等、init 后未声明 knowledge。

### 改动清单
1. `controller.py`：`cmd_init` 加 `bind_workspace_config` + `ensure_knowledge_base_for_init` 自动绑定（attach 复用 cmd_init 故同时覆盖）；补 `ensure_stage_content_ready`（调 `validate.validate_stage_content`）并接入 `cmd_mark`/`cmd_close`。
2. `validate.py`：补 4b-3（`confirmed` 且已有 artifact 时做 PRD/review 内容校验），对齐 codex 703 行。
3. `contract_config.json`：补 3 条 stage gate（fixed full workflow→global；duplicate-action→prototype；subjective-only→prd）+ 将 workspace/knowledge 段替换为 codex 新版 4 条（global 15-18）。最终 global/stages 语义检查数与 codex 完全一致（21 条，逐条 diff 吻合）。
4. `smoke_test.py`：整文件同步 codex 新版（含 bind_no_knowledge_base、test_workspace_rebind、test_knowledge_rebind、test_knowledge_bind_boundary_change_requires_rebind、write_boundary/prd/review 自包含函数），仅改 `prototype.css`→`prototype-chrome.css` 适配 workbuddy 资产名。
5. 清理：删 `.DS_Store`、`skills/pm-flow/__pycache__`（保留 `.workbuddy/` 项目数据）。

### 验证
- `python3 -B skills/pm-flow/smoke_test.py` → `pm-flow all smoke tests passed`，EXIT=0。
- 清理后残留检查为空（无 .DS_Store/__pycache__/*.pyc，排除 .workbuddy）。

### 结论
workbuddy 在治理逻辑/测试/contract 上已与 codex 当前版完全对齐，design-system 仍为独有优势。
