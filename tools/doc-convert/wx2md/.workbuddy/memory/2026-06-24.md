# 2026-06-24 工作日志

## wx2md 增强副标题元信息提取
- **需求**：微信文章副标题区域（原创、发布时间、地区、阅读数）需要完整提取并存入 Markdown frontmatter
- **改动文件**：`wx2md.py`
- **改动内容**：
  - 发布时间提取从单一选择器改为多层兜底策略：`#publish_time` → meta text 中正则匹配 `YYYY年MM月DD日 HH:mm` → 宽松日期格式
  - 新增 `is_original`（原创标识）提取
  - 新增 `location`（地区）提取
  - 新增 `read_count`（阅读人数）提取
  - frontmatter 字段从 `date` 改为 `pub_date`，并按需追加 `is_original`/`location`/`read_count`

## wx2md 调整：去掉 is_original/location，统一时间格式
- **需求调整**：`is_original` 和 `location` 不需要存入 MD，发布时间统一为 `YYYY-MM-DD HH:MM:SS` 格式
- **改动**：
  - 删除 `is_original`、`location` 的提取逻辑和 frontmatter 写入
  - 新增 `normalize_pub_date()` 函数，支持多种日期格式转标准格式，秒数缺失时自动补 `:00`
  - `pub_date` 在写入 frontmatter 前调用 `normalize_pub_date()` 标准化
  - frontmatter 仅保留 `read_count`（按需）
